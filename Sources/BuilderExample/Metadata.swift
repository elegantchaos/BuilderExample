
struct Metadata {
    let version: String
    let build: String
    let tags: String
    let commit: String
}

let BuilderExampleMetadata = Metadata(version: "1.0", build: "123", tags: "master", commit: "12343")
